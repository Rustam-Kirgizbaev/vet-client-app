import 'package:flutter/material.dart';

//client side colors
const mainBackgroundColor = Color(0xffEEEBEA);
const appBarColor = Color(0xffD8C6B0);
const panelNHeaderColor = Color(0xff494846);
//almost all texts are in "panelNHeaderColor"
const borderColor = Color(0xff707070);
const buttonColor = Color(0xffA69581);
const hintTextColor = Color(0xffB6B1B1);
const forgotPassColor = Color(0xff938E8E);
const currentMenuItem = Color(0xffDED1C2);
//currentMenuItem color used for our staff "More" btn background color
//make appointment page has the same colors with signup/login pages
//pasda uje make appointment field colorlari, inputla kiritilgandan keyin black bolishi kere, kiritilmaganda pasdigi rangla...
const fieldSelectedItem = Color(
    0xffD6C6B3); //only applicable if selecting is possible, just like the notification drop down table, i think you can skip this
const fieldBorderColor = Color(
    0xffA69581); //though other similar colors above can be used for example, hint color

//my account pages colors
const bottomNavBarCurrentPage = Color(0xffEBE6E2);
const bottomNavBarColor = Color(0xffD6C6B3);
//texts are the same "panelNHeader"
//notifications page can be copy pasted from vet side with minor changes
//now on "personal info" page
const personalInfoCard = Color(0xffECE4DB);
//for body texts of personal info page in client side please use the same same colors as in "My acc" of vet side

//my acc edit/add cards background color below
const dropWindowBackColor = Color(0xffEEEBEA);

//my pets page should have the same colors as in personal info
//brown buttons the same, need to use "buttonColor"
const deleteBtn = Color(0xffB8B3B3);

const petItalicTextColor = Color(0xff6F6D6A);
